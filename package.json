{
  "name": "sotto",
  "version": "1.0.0",
  "description": "Voice input for Claude Code — local speech-to-text via whisper.cpp",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "sotto": "dist/index.js",
    "sotto-setup": "dist/setup.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "setup": "node dist/setup.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/ test/"
  },
  "keywords": [
    "mcp",
    "whisper",
    "speech-to-text",
    "voice",
    "claude",
    "claude-code"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
